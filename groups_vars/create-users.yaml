---
- name:  Create Users & Groups
  hosts: target1
  gather_facts: false
  tasks:
    - name: Create Users Task
      user:
        name: "{{ item }}"
        state: present
        password: "{{ defaultuserpassword | password_hash('sha512', 'A512') }}"
        shell: /bin/bash
        group: "{{ devops }}"
      loop:
        - bob
        - wendy
        - lofty
        - dizzy